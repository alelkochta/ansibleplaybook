---
- name: ensure firewalld is installed
  become: true
  package:
    name: firewalld
    state: present

- name: start firewalld
  become: true
  ansible.builtin.systemd:
    name: firewalld
    state: started
    enabled: true
